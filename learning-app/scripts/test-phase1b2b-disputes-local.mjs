import{access,readFile}from"node:fs/promises";import{spawn}from"node:child_process";import path from"node:path";import{fileURLToPath}from"node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const name="supabase_db_growvelt-learning-phase0c-local";const candidates=process.platform==="win32"?[process.env.DOCKER_PATH,path.join(process.env.LOCALAPPDATA??"","Programs","DockerDesktop","resources","bin","docker.exe")].filter(Boolean):["docker"];let docker="docker";for(const c of candidates){try{if(c!=="docker")await access(c);docker=c;break}catch{}}
function run(args,input=""){return new Promise((resolve,reject)=>{const child=spawn(docker,args,{cwd:root,stdio:["pipe","pipe","pipe"],shell:false});let out="",err="";child.stdout.on("data",c=>out+=c);child.stderr.on("data",c=>err+=c);child.once("error",reject);child.once("exit",code=>code===0?resolve(out.trim()):reject(new Error((err||out).trim())));child.stdin.end(input)})}
const target=await run(["inspect","--format","{{.Name}}|{{.State.Status}}|{{(index (index .NetworkSettings.Ports \"5432/tcp\") 0).HostPort}}",name]);if(target!==`/${name}|running|55432`)throw new Error(`Refusing unexpected database target: ${target}`);
const migration=await readFile(path.join(root,"supabase","migrations","20260831000000_add_dispute_chargeback_foundation.sql"),"utf8");
for(const functionName of ["prevent_learning_payment_case_identity_change","receive_paystack_test_dispute_event","finalize_paystack_test_chargeback","process_paystack_test_dispute_event","receive_paystack_test_verified_dispute","get_learning_dispute_case_for_recovery","list_learning_dispute_cases","reconcile_paystack_test_disputes"]){const start=migration.indexOf(`create or replace function public.${functionName}`),marker="$function$;",end=migration.indexOf(marker,start);if(start<0||end<0)throw new Error(`Missing ${functionName}`);await run(["exec","-i",name,"psql","-X","-v","ON_ERROR_STOP=1","-U","postgres","-d","postgres"],migration.slice(start,end+marker.length));}
const tests=await readFile(path.join(root,"supabase","tests","phase1b2b_disputes.sql"),"utf8");await run(["exec","-i",name,"psql","-X","-v","ON_ERROR_STOP=1","-U","postgres","-d","postgres"],tests);console.log("PASS Phase 1B2B disputes and chargebacks on isolated local Supabase");
