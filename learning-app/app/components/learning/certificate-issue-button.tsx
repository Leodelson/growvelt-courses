"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { ActionButton } from "@/app/components/ui/action-button";
import { InlineFeedback } from "@/app/components/ui/inline-feedback";
import { useLanguage } from "@/app/components/language-provider";
import { createClient } from "@/app/lib/supabase/browser";
export function CertificateIssueButton({ courseId }: { courseId: number }) { const router = useRouter(); const { locale } = useLanguage(); const text = locale === "fr" ? { get: "Obtenir le certificat", pending: "Création du certificat…", error: "Nous n’avons pas pu délivrer votre certificat. Réessayez." } : locale === "es" ? { get: "Obtener certificado", pending: "Emitiendo certificado…", error: "No pudimos emitir tu certificado. Inténtalo de nuevo." } : { get: "Get certificate", pending: "Issuing certificate…", error: "We couldn’t issue your certificate. Please try again." }; const [pending,setPending]=useState(false); const [error,setError]=useState<string|null>(null); async function issue(){if(pending)return;setPending(true);setError(null);const {data,error:rpcError}=await createClient().rpc("issue_own_learning_certificate",{p_course_id:courseId});setPending(false);if(rpcError){setError(text.error);return;}const code=data?.[0]?.certificate_code;if(code)router.push(`/dashboard/certificates/${encodeURIComponent(code)}`);else router.refresh();} return <div className="certificate-issue-action"><ActionButton className="button button-primary" type="button" isPending={pending} pendingLabel={text.pending} onClick={issue}>{text.get}</ActionButton>{error&&<InlineFeedback variant="error">{error}</InlineFeedback>}</div>; }
