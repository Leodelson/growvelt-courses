import { NextResponse } from "next/server";
import { isSameOriginRequest } from "@/app/lib/security/request-origin";
import { createClient } from "@/app/lib/supabase/server";
export async function POST(request: Request, { params }: { params: Promise<{ workspaceId: string; memberId: string }> }) { if (!isSameOriginRequest(request)) return NextResponse.json({ code: "invalid_origin" }, { status: 403 }); const { workspaceId, memberId } = await params; const workspace = Number(workspaceId); if (!Number.isSafeInteger(workspace) || !/^[0-9a-f-]{36}$/i.test(memberId)) return NextResponse.json({ code: "invalid_member" }, { status: 400 }); const supabase = await createClient(); if (!(await supabase.auth.getUser()).data.user) return NextResponse.json({ code: "unauthorized" }, { status: 401 }); const { error } = await supabase.rpc("remove_learning_company_member", { p_workspace_id: workspace, p_member_id: memberId }); if (error) return NextResponse.json({ code: "company_access_denied" }, { status: error.code === "42501" ? 403 : 409 }); return NextResponse.json({ ok: true }); }
