import { Skeleton } from "@/app/components/ui/skeleton";

export default function DashboardAdminInstructorQueueLoading() {
  return <section className="admin-page section-shell" aria-busy="true"><p className="sr-only" role="status">Loading Instructor applications…</p><header className="admin-page-header"><Skeleton className="skeleton-eyebrow" /><Skeleton className="skeleton-title skeleton-title-wide" /><Skeleton className="skeleton-copy" /><Skeleton className="skeleton-copy skeleton-copy-short" /></header><section className="operations-skeleton-controls"><Skeleton className="skeleton-input" /></section><section className="admin-application-list admin-queue-skeleton-list" aria-hidden="true">{Array.from({ length: 3 }, (_, index) => <article className="admin-application-row" key={index}><div><Skeleton className="skeleton-status" /><Skeleton className="skeleton-row-title" /><Skeleton className="skeleton-copy skeleton-copy-short" /><Skeleton className="skeleton-copy" /></div><div className="admin-row-meta"><Skeleton className="skeleton-date" /><Skeleton className="skeleton-button" /></div></article>)}</section></section>;
}
