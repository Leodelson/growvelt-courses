import { Skeleton } from "@/app/components/ui/skeleton";

export default function DashboardAdminInstructorDetailLoading() {
  return <section className="admin-page section-shell" aria-busy="true"><p className="sr-only" role="status">Loading Instructor application…</p><header className="admin-page-header"><Skeleton className="skeleton-eyebrow" /><Skeleton className="skeleton-title skeleton-title-wide" /><Skeleton className="skeleton-date" /></header><div className="admin-detail-grid" aria-hidden="true"><section className="admin-application-detail skeleton-card">{Array.from({ length: 5 }, (_, index) => <div className="skeleton-field" key={index}><Skeleton className="skeleton-eyebrow" /><Skeleton className={index > 2 ? "skeleton-textarea" : "skeleton-copy"} /></div>)}<Skeleton className="skeleton-status" /></section><section className="admin-review-panel skeleton-card"><Skeleton className="skeleton-eyebrow" /><Skeleton className="skeleton-title" /><Skeleton className="skeleton-copy" /><Skeleton className="skeleton-textarea" /><div className="skeleton-action-row"><Skeleton className="skeleton-button" /><Skeleton className="skeleton-button" /></div></section></div></section>;
}
