import { Skeleton } from "@/app/components/ui/skeleton";

export default function DashboardInstructorLoading() {
  return <section className="instructor-analytics-page section-shell instructor-analytics-loading" aria-busy="true"><p className="sr-only" role="status">Loading Instructor workspace…</p><header className="instructor-analytics-hero"><div><Skeleton className="skeleton-eyebrow" /><Skeleton className="skeleton-title skeleton-title-wide" /><Skeleton className="skeleton-copy" /><Skeleton className="skeleton-copy skeleton-copy-short" /></div><div className="skeleton-action-row"><Skeleton className="skeleton-button" /><Skeleton className="skeleton-button" /></div></header><div className="instructor-analytics-summary">{Array.from({ length: 4 }, (_, index) => <article className="instructor-analytics-stat-skeleton" key={index}><Skeleton className="skeleton-label" /><Skeleton className="skeleton-stat" /><Skeleton className="skeleton-copy-short" /></article>)}</div><section className="instructor-analytics-courses instructor-analytics-courses-skeleton"><Skeleton className="skeleton-eyebrow" /><Skeleton className="skeleton-row-title" />{Array.from({ length: 2 }, (_, index) => <article key={index}><Skeleton className="skeleton-status" /><Skeleton className="skeleton-row-title" /><Skeleton className="skeleton-copy" /><Skeleton className="skeleton-copy skeleton-copy-short" /></article>)}</section></section>;
}
