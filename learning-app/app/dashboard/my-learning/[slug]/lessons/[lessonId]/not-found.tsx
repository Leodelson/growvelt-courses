import Link from "next/link";
import { getRequestLocale } from "@/app/lib/i18n-server";
export default async function LessonNotFound() { const locale = await getRequestLocale(); const text = locale === "fr" ? { eyebrow: "Leçon indisponible", title: "Cette leçon n’est pas disponible pour ce compte.", copy: "Elle n’est peut-être plus publiée ou ce compte n’a pas d’inscription active.", action: "Retour à Mon apprentissage" } : locale === "es" ? { eyebrow: "Lección no disponible", title: "Esta lección no está disponible para esta cuenta.", copy: "Puede que ya no esté publicada o que esta cuenta no tenga una inscripción activa.", action: "Volver a Mi aprendizaje" } : { eyebrow: "Lesson unavailable", title: "This lesson isn’t available to this account.", copy: "It may no longer be published, or this account does not have active enrollment.", action: "Return to My Learning" }; return <section className="route-feedback-page section-shell"><section className="route-feedback-panel"><p className="eyebrow">{text.eyebrow}</p><h1>{text.title}</h1><p>{text.copy}</p><Link className="button button-primary" href="/dashboard/my-learning">{text.action}</Link></section></section>; }
